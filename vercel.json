{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "frontend" } }
  ],
  "routes": [
    { "src": "/(.*\\.(css|js|png|jpg|jpeg|svg|ico))", "dest": "/frontend/$1" },
    { "handle": "filesystem" },
    { "src": "^/favicon.ico$", "dest": "/frontend/favicon.ico" },
    { "src": "/(.*)", "dest": "/frontend/index.html" }
  ]
}
